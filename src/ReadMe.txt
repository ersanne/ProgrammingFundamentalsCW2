/////////////////////////////////////////////////////////////////////////////

Author: Erik Sanne
Last modified: 01/04/2017

The test.cpp and linked_list.h files have not been modified.
The LinkedList.cpp file was last modified by Erik Sanne on 01/04/2017.

The compiler used was the Microsoft (R) C/C++ Optimizing Compiler Version 19.00.24215.1 for x86.

/////////////////////////////////////////////////////////////////////////////

Makefile:

The linked list is always compiled into a library.

The makfile can be used to:

test -> Compiles the whole programm but doesn't execute it. 
testrun -> Compiles and executes the programm.
linkedlistcl -> Compiles the LinkedList.cpp into a library.
testcl -> Compiles the test.cpp
clean -> Deletes all obj lib and exe files.

/////////////////////////////////////////////////////////////////////////////